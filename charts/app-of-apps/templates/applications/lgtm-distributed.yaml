{{ if .Values.applications.lgtm.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lgtm-distributed
  namespace: {{ .Values.applications.argocd.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.applications.lgtm.wave | quote }}
  labels:
    {{- include "app-of-apps.labels" . | nindent 4 }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.applications.lgtm.namespace }}
  project: {{ .Values.project.name }}
  source:
    path: charts/lgtm-distributed-{{ .Values.applications.lgtm.revision }}/lgtm-distributed
    repoURL: {{ .Values.project.repository }}
    targetRevision: {{ .Values.project.branch }}
    helm:
      valueFiles:
        - values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    managedNamespaceMetadata:
      labels:
        istio-injection: enabled
{{ end }}
